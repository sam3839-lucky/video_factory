# 视频自动化发布流程方案

## 目标

通过飞书多维表格管理视频文案，实现：
1. 文案自动读取
2. 视频自动生成
3. 定时发布
4. 飞书通知

---

## 一、多维表格结构

**表格**：`视频号发布记录`
**Base Token**：`XX8abIKw7a9GwBsVt57crlbHnOe`
**表名**：`发布记录`
**Table ID**：`tblHptO4dDJckuFF`

### 现有字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 视频描述 | text | |
| 发布状态 | select | pending / uploading / published / failed / archived |
| 归档时间 | datetime | |
| 视频文件路径 | text | |
| 发布时间 | datetime | 用于定时发布 |
| 错误信息 | text | |
| 创建时间 | datetime | |
| 视频类型 | select | 日报 / 周报 / 月报 / 专题 |
| 文件大小MB | number | |
| 视频号链接 | text | 发布后填写 |
| 归档天数 | number | |
| 视频标题 | text | |

### 待新增字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 文案内容 | text | 视频配音文案，从这里读取（已存在） |
| 视频链接 | text | 生成后填入，公网预览链接（已存在） |
| 下架时间 | datetime | 计划下架时间，用于触发自动下架（已存在） |
| 下架操作时间 | datetime | 实际下架时间，记录实际操作时间（已存在） |
| 视频标签 | text | 视频号发布时的标签，多个用逗号分隔（待新增） |
| 原创标志 | select | 是/否，标记视频是否原创（待新增） |

---

## 二、本地文件结构

```
~/Videos/
├── drafts/           # 待发布视频
│   └── 深圳楼市_20260423.mp4
└── archived/         # 已发布归档
    └── 深圳楼市_20260422.mp4
```

**规则**：
- 视频文件名：`{视频类型}_{日期}.mp4`，如 `日报_20260423.mp4`
- 生成后暂存 drafts/
- 发布成功后移动到 archived/

---

## 三、自动化流程（Cron）

### 触发逻辑
- Cron 任务每 30 分钟运行一次
- 扫描多维表格：`发布状态=pending` 且 `发布时间≤当前时间`
- 按发布时间排序，先发布的先处理

### 执行步骤

```
[1] 读取待发布记录
    └── 从多维表格读取 文案内容、发布状态、发布时间

[2] 生成视频
    ├── 调用 generate_video.py，传入文案
    ├── 输出到 ~/Videos/drafts/{文件名}.mp4
    └── 字幕数字转换（保留习惯用语）

[3] 上传视频号
    ├── 调用 wechat-video-publish skill
    ├── 上传到微信视频号平台
    └── 获取视频号链接

[4] 更新多维表格
    ├── 发布状态 → published
    ├── 发布时间 → 实际发布时间
    ├── 视频文件路径 → 实际路径
    ├── 视频号链接 → 视频号URL
    └── 视频链接 → 公网预览链接（用于飞书通知）

[5] 移动文件
    └── drafts/ → archived/

[6] 飞书通知
    └── 发送消息给波哥：视频标题 + 视频号链接 + 公网预览链接
```

---

### 4.2 视频生成
- 脚本：`/Users/sam/video_factory/generate_video.py`
- 输入：文案文本（来自多维表格）
- 输出：`~/Videos/drafts/{文件名}.mp4`
- TTS：say -v Tingting（腾讯云TTS后续接入）
- 字幕：中文数字转阿拉伯数字，习惯用语保留

### 4.3 视频号发布（浏览器自动化）
**发布内容（必填）**：
- 视频文件：本地视频文件
- 视频描述：来自多维表格「视频描述」字段
- 视频短标题：来自多维表格「视频标题」字段
- 视频标签：来自多维表格（需新增「视频标签」字段）
- 视频原创标志：来自多维表格（需新增「原创标志」字段，true/false）
- 视频类型：来自多维表格「视频类型」字段（日报/周报/月报/专题）

**说明**：视频号无公开API支持发布内容填写，需使用浏览器自动化（Playwright/CDP）操作视频号后台发布页面。

### 4.4 公网预览链接
- 使用 `localhost.run` 穿透
- 生成 https 链接填入多维表格「视频链接」字段

### 4.5 定时任务
```bash
# 每30分钟执行
*/30 * * * * /Users/sam/video_factory/cron_publish.py
```

---

## 五、Cron 脚本功能

`cron_publish.py`：

1. **初始化**
   - 加载 lark-cli 配置
   - 连接多维表格

2. **扫描待发布记录**
   - 查询 `发布状态=pending` 且 `发布时间≤now`
   - 按发布时间升序

3. **逐条处理**
   - 对于每条记录：
     a. 读取文案内容
     b. 调用 generate_video.py 生成视频
     c. 调用视频号发布脚本上传
     d. 生成本地预览链接（localhost.run）
     e. 更新多维表格（状态、链接、发布时间）
     f. 移动视频文件到 archived/
     g. 发送飞书通知

4. **飞书通知内容**
   ```
   🎬 视频已发布！
   标题：{视频标题}
   类型：{视频类型}
   视频号：{链接}
   预览：{公网链接}
   ```

---

## 六、字段变更记录

### 新增字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 文案内容 | text | 存储视频配音文案 |
| 视频链接 | text | 公网预览链接 |

### 待更新字段（自动化写入）

| 字段名 | 自动化写入值 |
|--------|-------------|
| 发布状态 | pending → published |
| 发布时间 | 自动化发布时的实际时间 |
| 视频文件路径 | 实际文件路径 |
| 视频号链接 | 视频号平台返回的链接 |
| 视频链接 | 公网预览链接 |

---

## 六、自动下架流程

### 触发逻辑
- Cron 任务扫描 `发布状态=published` 且 `下架时间≤当前时间` 的记录
- 满足条件时执行下架

### 执行步骤

```
[1] 查询待下架记录
    └── 状态=published 且 下架时间≤now

[2] 视频号下架
    ├── 方式A（优先）：视频号官方API（需调研是否存在）
    └── 方式B（兜底）：浏览器自动化操作
        - 登录视频号后台
        - 进入内容管理
        - 找到对应视频
        - 点击下架/删除

[3] 更新多维表格
    ├── 发布状态 → archived
    ├── 下架操作时间 → 当前时间
    └── 归档时间 → 当前时间

[4] 移动文件（可选）
    └── drafts/ → archived/
```

### 视频号下架方式调研

**方式A：官方API**
- 视频号目前无公开的下架API
- 企业微信/服务商可能有管理接口（需确认）

**方式B：浏览器自动化**
- 登录 `channels.weixin.qq.com` 或视频号助手网页版
- 通过页面操作下架视频
- 需要处理登录态（Cookie/Session）

---

## 七、风险与注意事项

1. **TTS 额度**：腾讯云TTS额度用完时，临时用 say 命令替代
2. **视频号上传**：依赖浏览器自动化，需确保 Chrome 可用
3. **视频号下架**：无官方API，优先调研是否有企业接口，无则用浏览器自动化
4. **并发处理**：同一时间段多条待发布/待下架视频时，串行处理避免冲突
5. **失败重试**：发布/下架失败时状态改为 failed，错误信息写入对应字段
6. **文件命名**：避免文件名重复，已发布时间戳区分

---

## 八、开发顺序

| 步骤 | 内容 | 状态 |
|------|------|------|
| 1 | 多维表格新增字段（文案内容、视频链接、下架时间、下架操作时间、视频标签、原创标志） | ✅ 已完成 |
| 2 | 编写 cron_publish.py 主脚本 | ✅ 框架已完成 |
| 3 | 测试：读取多维表格 → 生成视频 | ⬜ 待测试 |
| 4 | 测试：公网预览链接 | ⬜ 待测试 |
| 5 | 测试：更新多维表格 | ⬜ 待测试 |
| 6 | 测试：飞书通知 | ⬜ 待测试 |
| 7 | 设置 Cron 定时任务 | ⬜ 待配置 |
| 8 | 视频号自动化发布（浏览器自动化） | 🔄 调研中（可行性确认，待实现） |
| 9 | 视频号自动化下架（浏览器自动化） | 🔄 调研中（可行性确认，待实现） |

## 九、当前状态

**已完成**：
- 多维表格字段（文案内容、视频链接、下架时间、下架操作时间、视频标签、原创标志）
- cron_publish.py 框架（生成视频 → 公网链接 → 通知波哥）
- generate_video.py 支持命令行参数传入文案和输出路径

**进行中**：
- 视频号浏览器自动化调研（官方无API，需Playwright）

**当前流程（半自动）**：
```
多维表格（待发布记录）
       ↓  Cron 定时触发
读取文案 → 生成视频 → 公网预览链接 → 飞书通知波哥
       ↓
波哥手动发布视频到视频号
       ↓
波哥手动更新多维表格（填入视频号链接、发布时间）
```

**目标流程（全自动）**：
```
多维表格（待发布记录）
       ↓  Cron 定时触发
读取文案 → 生成视频 → 上传视频号 → 更新表格 → 飞书通知
       ↓
定时下架 → 更新表格 → 飞书通知
```
