# 视频自动化发布流程方案

## 目标

通过飞书多维表格管理视频文案，实现：
1. 文案自动读取
2. 视频自动生成
3. 定时发布
4. 飞书通知

---

## 一、多维表格结构

**表格**：`视频号发布记录`
**Base Token**：`XX8abIKw7a9GwBsVt57crlbHnOe`
**表名**：`发布记录`
**Table ID**：`tblHptO4dDJckuFF`

### 现有字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 视频描述 | text | |
| 发布状态 | select | 草稿 / 待制作 / 制作中 / 待下载 / 下载中 / 待发布 / 发布中 / 已发布 / 已归档 / 生成失败 / 发布失败 / 上传失败 |
| 归档时间 | datetime | |
| 视频文件路径 | text | |
| 发布时间 | datetime | 用于定时发布 |
| 错误信息 | text | |
| 创建时间 | datetime | |
| 视频类型 | select | 日报 / 周报 / 月报 / 专题 |
| 文件大小MB | number | |
| 视频号链接 | text | 发布后填写 |
| 归档天数 | number | |
| 视频标题 | text | |

### 待新增字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 文案内容 | text | 视频配音文案，从这里读取（已存在） |
| 视频链接 | text | 生成后填入，公网预览链接（已存在） |
| 下架时间 | datetime | 计划下架时间，用于触发自动下架（已存在） |
| 下架操作时间 | datetime | 实际下架时间，记录实际操作时间（已存在） |
| 视频标签 | text | 视频号发布时的标签，多个用逗号分隔（待新增） |
| 原创标志 | select | 是/否，标记视频是否原创（待新增） |

---

## 二、本地文件结构

```
~/Videos/
├── drafts/           # 待发布视频
│   └── 深圳楼市_20260423.mp4
└── archived/         # 已发布归档
    └── 深圳楼市_20260422.mp4
```

**规则**：
- 视频文件名：`{视频类型}_{日期}.mp4`，如 `日报_20260423.mp4`
- 生成后暂存 drafts/
- 发布成功后移动到 archived/

---

## 三、自动化流程（Cron）

### 触发逻辑
- Cron 任务每 30 分钟运行一次
- 扫描多维表格：`发布状态=待制作` → 读取文案生成视频
- 并发锁：文件锁防止多个 cron 实例同时处理同一记录
- 过期清理：已发布视频按类型保留天数后自动归档/删除

### 执行步骤

**阶段一：视频生命周期（待制作 → 已发布）**

```
[1] 扫描待制作记录
    └── 查  发布状态=待制作
    └── 读取 文案内容、视频标题、视频类型

[2] 生成视频
    ├── 发布状态 → 制作中
    ├── 调用 MoneyPrinterV2，传入文案
    ├── 输出到 ~/Videos/drafts/{类型}_{日期}_{标题}.mp4
    └── 异常 → 发布状态 → 生成失败（记录错误信息）

[3] 文件就位
    ├── 发布状态 → 待下载 → 下载中 → 待发布
    └── 视频文件路径写入表格

[4] 发布到视频号
    ├── 发布状态 → 发布中
    ├── 调用视频号浏览器自动化发布
    ├── 获取视频号链接
    └── 异常 → 发布状态 → 发布失败（记录错误信息）

[5] 上传云盘预览 + 通知
    ├── 发布状态 → 已发布
    ├── 发布时间 → 当前时间
    ├── 视频链接 → 飞书云盘公网预览
    ├── 视频号链接 → 视频号URL
    └── 飞书通知波哥：标题 + 预览链接 + 视频号链接
    └── 异常 → 发布状态 → 上传失败（记录错误信息）
```

**阶段二：过期清理（定时）**

```
[6] 扫描已发布记录
    └── 按类型判断是否超期
    ├── 日报：3天 → 归档
    ├── 周报：5天 → 直接删除
    ├── 月报：10天 → 归档
    └── 专题：不处理

[7] 执行归档/删除
    ├── 更新发布状态 → 已归档
    ├── 填写下架时间、下架操作时间
    ├── 归档 → 移动到 archived/
    └── 删除 → 直接删除本地文件
```

---

### 4.2 视频生成
- 脚本：`/Users/sam/video_factory/generate_video.py`
- 输入：文案文本（来自多维表格）
- 输出：`~/Videos/drafts/{文件名}.mp4`
- TTS：say -v Tingting（腾讯云TTS后续接入）
- 字幕：中文数字转阿拉伯数字，习惯用语保留

### 4.3 视频号发布（浏览器自动化）
**发布内容（必填）**：
- 视频文件：本地视频文件
- 视频描述：来自多维表格「视频描述」字段
- 视频短标题：来自多维表格「视频标题」字段
- 视频标签：来自多维表格（需新增「视频标签」字段）
- 视频原创标志：来自多维表格（需新增「原创标志」字段，true/false）
- 视频类型：来自多维表格「视频类型」字段（日报/周报/月报/专题）

**说明**：视频号无公开API支持发布内容填写，需使用浏览器自动化（Playwright/CDP）操作视频号后台发布页面。

### 4.4 公网预览链接
- 使用 `localhost.run` 穿透
- 生成 https 链接填入多维表格「视频链接」字段

### 4.5 定时任务
```bash
# 每30分钟执行
*/30 * * * * /Users/sam/video_factory/cron_publish.py
```

---

## 五、Cron 脚本功能

`cron_publish.py`（支持 `--dry-run` 干跑测试）：

1. **初始化**
   - 加载 lark-cli 配置（环境变量或配置文件）
   - 文件锁：防止并发实例重复处理
   - 连接多维表格

2. **阶段一：视频生命周期**
   - 扫描 `发布状态=待制作`，逐条处理
   - 状态流转：待制作 → 制作中 → 待下载 → 下载中 → 待发布 → 发布中 → 已发布
   - 失败状态细分：生成失败 / 发布失败 / 上传失败
   - 每步更新表格状态，全程可追踪

3. **阶段二：过期清理**
   - 扫描 `发布状态=已发布` 且超过保留期的记录
   - 按类型执行归档或删除
   - 保留期：日报3天、周报5天、月报10天、专题不处理

4. **飞书通知内容**
   ```
   🎬 视频已发布！
   标题：{视频标题}
   类型：{视频类型}
   视频号：{链接}
   预览：{公网链接}
   ```

---

## 六、字段变更记录

### 已实现字段

| 字段名 | 类型 | 自动化写入值 |
|--------|------|-------------|
| 发布状态 | select | 状态流转全程追踪 |
| 发布时间 | datetime | 实际发布时间 |
| 视频文件路径 | text | 实际文件路径 |
| 视频号链接 | text | 视频号平台返回的链接 |
| 视频链接 | text | 飞书云盘公网预览链接 |
| 错误信息 | text | 失败时写入具体错误 |
| 下架时间 | datetime | 归档时填写 |
| 下架操作时间 | datetime | 归档时填写 |

### 发布状态完整选项

草稿 / 待制作 / 制作中 / 待下载 / 下载中 / 待发布 / 发布中 / 已发布 / 已归档 / **生成失败** / **发布失败** / **上传失败**

---

## 七、过期清理规则

| 视频类型 | 保留期 | 超期后动作 |
|---------|--------|-----------|
| 日报 | 3天 | 归档（移动到 archived/） |
| 周报 | 5天 | 直接删除 |
| 月报 | 10天 | 归档（移动到 archived/） |
| 专题 | 不处理 | — |

## 八、风险与注意事项

1. **TTS 额度**：腾讯云TTS额度用完时，临时用 say 命令替代
2. **视频号上传**：依赖浏览器自动化，需确保 Chrome 可用
3. **视频号下架**：无官方API，当前为手动下架（后续可调研浏览器自动化）
4. **并发处理**：文件锁防止多个 cron 实例同时处理同一记录
5. **失败状态**：细分到环节（生成失败/发布失败/上传失败），错误信息写入 `错误信息` 字段
6. **文件命名**：避免文件名重复，以时间戳区分

---

## 八、开发顺序

| 步骤 | 内容 | 状态 |
|------|------|------|
| 1 | 多维表格新增字段（文案内容、视频链接、下架时间、下架操作时间、视频标签、原创标志） | ✅ 已完成 |
| 2 | 编写 cron_publish.py 主脚本 | ✅ 框架已完成 |
| 3 | 测试：读取多维表格 → 生成视频 | ⬜ 待测试 |
| 4 | 测试：公网预览链接 | ⬜ 待测试 |
| 5 | 测试：更新多维表格 | ⬜ 待测试 |
| 6 | 测试：飞书通知 | ⬜ 待测试 |
| 7 | 设置 Cron 定时任务 | ⬜ 待配置 |
| 8 | 视频号自动化发布（浏览器自动化） | 🔄 调研中（可行性确认，待实现） |
| 9 | 视频号自动化下架（浏览器自动化） | 🔄 调研中（可行性确认，待实现） |

## 九、当前状态

**已完成**：
- cron_publish.py 重写（状态流转 + 过期清理 + 并发锁 + --dry-run）
- 飞书多维表格字段（文案内容、视频链接、下架时间、下架操作时间、视频标签、原创标志）
- 失败状态细分（生成失败/发布失败/上传失败）+ 飞书表格选项已更新
- token 环境变量读取（不硬编码）
- 并发锁防重（fcntl 文件锁）
- 干跑测试模式（`--dry-run`）

**进行中**：
- 视频号浏览器自动化发布（官方无API，需Playwright）

**当前流程（全自动）**：
```
多维表格（待制作）→ Cron触发
       ↓
读取文案 → 生成视频 → 发布到视频号 → 飞书云盘预览 → 飞书通知波哥
       ↓
已发布 → 超期后自动归档/删除
```
